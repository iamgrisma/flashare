# Project Rules

- **Do NOT run automated browser subagents or test URLs/local dev servers yourself.** It wastes heavy tokens.
- Always ask the USER to test URLs or local endpoints on their own devices.
